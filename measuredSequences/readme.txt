The first column is the node orders, the second column is the ConGra measure value for each node, 

and the remaining columns are the measure values from comparison algorithms

The csv file naming format is: Network name_parameter t value.
